#!/usr/bin/env python3
"""Console entrypoint installed via CMake (PROGRAMS) so that
`ros2 run lidar_camera_fusion scan_cycle_node` Just Works."""
from lidar_camera_fusion.scan_cycle_node import main

if __name__ == "__main__":
    main()
